1. Problem by upload photo in Angular not working???
   a. in photo-editor.ts:

     initializeUploader() {
        this.uploader = new FileUploader({
   -->    url: this.baseUrl + 'users/add-photo',   // after was empty!!! baseUrl + ''
          authToken: 'Bearer ' + this.user.token,
          isHTML5: true,
          allowedFileType: ['image'],
          removeAfterUpload: true,
          autoUpload: false,
          maxFileSize: 10 * 1024 * 1024
        });
